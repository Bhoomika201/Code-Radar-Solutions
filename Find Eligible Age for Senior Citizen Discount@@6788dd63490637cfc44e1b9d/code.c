#include <stdio.h>

int main() {
    int n ;
    printf(" ",n);
    scanf("%d",&n);
    if(n >= 65){
        printf("Eligible");
    }
    else{
        printf("Not eligible");
    }
    return 0;
}