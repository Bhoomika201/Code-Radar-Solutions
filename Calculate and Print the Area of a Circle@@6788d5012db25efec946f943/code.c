#include <stdio.h>

int main() {
    double radius;
    float pi =3.14;
    printf(" ",radius);
    scanf("%lf",&radius);
    area = pi * radius * radius;
    printf("Area : %lf", area);
    return 0;
}