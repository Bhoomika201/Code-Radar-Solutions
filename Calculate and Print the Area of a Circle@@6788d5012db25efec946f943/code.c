#include <stdio.h>

int main() {
    float radius,area;
    float pi = 3.14;
    printf(" ",radius);
    scanf("%f",&radius);
    area = pi * radius * radius;
    printf("Area: %.2f", area);
    return 0;
}